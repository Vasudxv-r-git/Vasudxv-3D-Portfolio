# Cinematic Redesign — Centered Nav, Feathered Portrait, Full-Screen Sections

Modify the existing site (no rebuild, content unchanged). Theme, fonts, starfield, animations, and purple/blue identity all stay.

## 1. Portrait asset (one-off, via sharp)
- Center-crop `public/profile.png` (1254²) to a **4:5 vertical** (~1000×1250).
- Bake **feathered alpha edges** into the PNG (gradient fade ~12% on all sides) so the person emerges from the starfield with no box, no circle, no border.
- Save as `public/portrait.png`; add `PROFILE.portrait` to constants.

## 2. Navbar → top-center floating glass pill (`components/main/navbar.tsx`)
- Nav pill absolutely centered (`left-1/2 -translate-x-1/2`), glass style: `backdrop-blur-xl`, translucent dark bg, thin `#7042f88b` glowing border, soft purple shadow.
- Logo/name stays minimal and separate (left); hamburger right on mobile.
- Active page keeps purple/blue glow indicator (brighter + text-shadow glow).
- Mobile: centered glass dropdown panel; fully responsive 320px→2560px.

## 3. Hero → split centered composition (`hero-content.tsx`, minor `hero.tsx`)
- `min-h-screen` flex, two-column group **visually centered** as a unit; stacks vertically on mobile.
- Left: chip → headline → intro → CTA.
- Right: 4:5 feathered portrait (~65–70vh), no frame, with layered **purple/blue atmospheric glow** behind it (blurred radial gradients), slight parallax float animation (subtle).
- Blackhole video stays full-bleed.

## 4. Full-screen centered sections (all pages)
Unified rules across About, Skills, Projects, Designs, Experience, Certifications:
- Sections `min-h-screen`, content vertically centered, `py-24` rhythm, wider `px` on large screens.
- Consistent wider containers: `max-w-7xl` for grids (Designs, Certifications, Pillars, Stats), `max-w-6xl` for About/Projects, roomier Experience timeline.
- Cards keep the existing glass/border style; only spacing, sizing, and centering change.
- `globals.css`: add portrait-glow + glass-pill utilities.

## 5. Verify
- Restart dev server (it was stopped), run `npm run build`, curl all 8 routes.
- Browser check at 1920×1080, 1280×720 and mobile viewport: centered nav, feathered portrait, no horizontal scroll, screenshots as evidence.